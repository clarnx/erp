import { NextPage } from "next";
import React from "react";

import { withAuth } from "@/utils/withAuth";

const WeeklyShippingPage: NextPage = () => {
  return <div>WeeklyShipping</div>;
};

export default withAuth(WeeklyShippingPage);
